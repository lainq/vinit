// Create and remove insufficent data strucrures
// from the code ans optimize the data for
// people to use as a good thing
fn createDataLog(n int, y int) int {
	for data := n; data < n+y; data++ {
		if data == (y/(n+n)) {
			break;
		}
	}

}